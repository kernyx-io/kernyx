"use client"

import type { CalculatorState } from "../lib/types"

type Props = {
  state: CalculatorState
  dispatch: React.Dispatch<any>
}

export default function CrewSelection({ state, dispatch }: Props) {
  return (
    <div>
      {/* UI here */}
    </div>
  )
}
